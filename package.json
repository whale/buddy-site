{
  "name": "buddy-site",
  "private": true,
  "description": "Kuma / Buddy landing page. The page itself has no build step; this file exists only so Vercel installs the one dependency the signup function needs.",
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  }
}
